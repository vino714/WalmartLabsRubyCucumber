Steps to execute:

1.Install Ruby
2.Goto Command Prompt and type "gem install bundle"
3.Once installed, goto to cloned project location and type "bundle install"
4.This will install all required components
5.Once bundle install is done, type "cucumber" to execute the scenarios.